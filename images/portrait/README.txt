Drop your PORTRAIT photos in this folder (.jpg, .jpeg, .png, or .webp).
Then run the update script in the project root (double-click update-images.command
on Mac, or update-images.bat on Windows) and refresh the site.
